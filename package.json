{
  "name": "chatgpt-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension: discuss current page with ChatGPT",
  "scripts": {
    "clean": "node scripts/clean-dist.mjs",
    "build": "npm run clean && tsc && node scripts/copy-assets.mjs",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "typescript": "^5.8.3"
  }
}
